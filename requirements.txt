PySide6>=6.6.0
pymupdf>=1.24.0
pymorphy3>=2.0.2
pymorphy3-dicts-ru>=2.4.417150.4580142

# Optional — only needed for OCR on scanned PDFs.
# easyocr>=1.7.2          # heavy (~PyTorch, ~2 GB on first run)
# pytesseract>=0.3.10     # lighter; needs system 'tesseract' + Russian pack
# pillow>=10.0.0
